;;; disco-transient.el --- Shared transient support for disco.el -*- lexical-binding: t; -*-

;; Author: disco.el contributors

;;; Commentary:

;; Placeholder for transient infrastructure shared across disco subsystems.
;; Root and room command menus live with their owning modules; this file should
;; only grow when shared transient classes or helpers are needed.

;;; Code:

(provide 'disco-transient)

;;; disco-transient.el ends here
